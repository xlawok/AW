---
label: Autorzy
hide_body: false
fields:
- name: autor
  type: field_group_list
  fields:
  - name: author_name
    type: text
    config:
      required: false
    label: Imię i nazwisko
  - name: image
    type: file
    config:
      maxSize: 2
    label: Zdjęcie
  config:
    min: 
    max: 
    labelField: 
  label: Autor
pages:
- data/authors.toml
