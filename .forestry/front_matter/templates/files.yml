---
label: Files
hide_body: true
fields:
- name: dodaj_pliki
  type: field_group_list
  fields:
  - name: dodaj_plik
    type: file
    config:
      maxSize: 64
    label: Dodaj plik
  - name: rodzaj_pliku
    type: select
    default: []
    config:
      required: false
      options:
      - Skrypt
      - Styl
      source:
        type: simple
        section: 
        file: 
        path: 
    label: Rodzaj pliku
  config:
    min: 
    max: 
    labelField: 
  label: Dodaj pliki
pages:
- config.toml
